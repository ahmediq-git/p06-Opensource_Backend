class EzBaseClient {

    constructor(backendUrl){
        console.log("sdk ezbase client");
        this.backendUrl=backendUrl
    }

    sendToBackend(){
        //This function takes the json object and the api endpoint and sends it to the backend
    }
}

export default EzBaseClient;